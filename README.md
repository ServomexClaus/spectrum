# Chroma Argon Lines

Minimal Hello World starter project.

## Run

Open `index.html` in a browser.

## Plot Argon Spectra

Install dependencies:

`pip install numpy matplotlib`

Run the plotting script:

`python plot_spectra.py`
